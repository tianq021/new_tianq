FastGPT 工作流 JSON 自检清单（2026-05-12）

1. nodeId 全局唯一。
2. edges.source / edges.target 都存在。
3. 普通边 handle: 源节点ID-source-right；目标节点ID-target-left。
4. userSelect 分支: 用户选择节点ID-source-选项key。
5. tools 挂载: sourceHandle=selectedTools, targetHandle=selectedTools。
6. catchError 错误分支: 节点ID-source_catch-right。
7. loop 子节点必须 parentNodeId=loop节点ID，且被 loop.childrenNodeIdList 包含。
8. 引用 [nodeId, outputId] 时，outputId 必须存在于目标节点 outputs.id。
9. 文本变量 {{$nodeId.outputId$}} 同样检查 outputs.id。
10. workflowStart 没有 userFiles 时，不能引用 [workflowStart, userFiles]。
11. variableUpdate 没有 outputs，不要引用它的输出。
12. 企业微信/钉钉 webhook 只有 system_error_text，没有正常 result。
13. 飞书 webhook 有 result。
14. 数据库、webhook、appModule pluginId、secret、URL、密码等不要写死。
15. appModule.pluginId 必须来自真实应用导出。
16. formInput 字段改名后，以 outputs.id 为准。
17. formInput required 是必填开关；timeGranularity 已确认 hour/minute/second。
18. HTTP、数据库、系统工具 catchError 行为要按实际节点配置检查。
